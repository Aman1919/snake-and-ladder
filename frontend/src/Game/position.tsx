export default class Position {
       x: number;
       y: number;
        num: number; 
        constructor(x: number, y: number,num:number) {
                this.x = x;
                this.y = y;
                this.num = num;
    }
}